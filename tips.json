---
---
{
	"tips":
	{
		{% for tip in site.tags.tip %}{% if forloop.index != 1 %},{% endif %}"tip{{ forloop.index }}":{
			"body":"{{ tip.body }}",
			"category":"{{tip.categories}}"
		}{% endfor %}
	}
}